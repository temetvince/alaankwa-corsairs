# C++ ENTT and RayLib Template

## Running the project

Requires CMake & LLVM with tools like clang-format & scan-build.
Requires an internet connection the first run to download dependencies.

```bash
chmod +x run.sh
./run.sh
```
